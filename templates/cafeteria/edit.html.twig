{% extends 'base.html.twig' %}

{% block title %}Edit Cafeteria{% endblock %}

{% block body %}
    <h1>Edit Cafeteria</h1>

    {{ include('cafeteria/_form.html.twig', {'button_label': 'Refrescar'}) }}

    <a class="btn" style="background-color:rgb(156, 127, 92);color: white;" href="{{ path('app_cafeteria_index') }}">Volver</a>

    {{ include('cafeteria/_delete_form.html.twig') }}
{% endblock %}
